// variables declared in If/Else form

// Switch
/*
switch(stateAbrev) {
  case "NE":
    if (userTemp < 32)
      console.log(`${firstName}), ${messages[0]}`);
    else if (userTemp >=32 && userTemp <= 50)
      console.log(`${firstName}), ${messages[1]}`)
  break;
  case "FL":
    if (userTemp >= 32 && userTemp <= 50)
      console.log(`${firstName}), ${messages[2]}`);
    else if (userTemp > 50 && userTemp <= 70)
      console.log(`${firstName}), ${messages[3]}`)
    break;
  default:
    console.log("Try again..");
    break;
*/